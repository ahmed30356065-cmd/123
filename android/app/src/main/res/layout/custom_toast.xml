<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/toast_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:elevation="6dp">

    <!-- Simple Exit Icon (Using standard android drawable or text as icon) -->
    <!-- Ideally we use a drawable, but for simplicity/robustness without adding new assets, we'll use a text emoji or basic shape if icon unavailable. 
         Actually, we can use a built-in android icon or just text. Let's make it text-only but styled elegantly to avoid missing asset crashes. -->
         
    <TextView
        android:id="@+id/toast_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="اضغط مرة أخرى للخروج"
        android:textColor="#ffffff"
        android:textSize="14sp"
        android:fontFamily="@font/custom_font"
        android:textStyle="bold"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"/>

</LinearLayout>
