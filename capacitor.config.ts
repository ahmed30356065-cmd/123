
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.goo.now.a',
    appName: 'GOO NOW',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
